import java.util.ArrayList;

public interface Sort {
    void sort(ArrayList<Calabash> calabashes, int[] positions);

    void sort(GrandPa grandPa, ArrayList<Calabash> calabashes, int[] positions);
}
